import executor

# Define the command to create the file
command = 'arbol "./"'

# Call the handle_command function with the command
#result = executor.handle_command(command)

# Print the result
print(executor.handle_command(command))
